flask==3.1.1
flask-cors==5.0.1
python-dotenv==1.1.0

# MCP server + ASGI runtime for the unified local API (Flask + MCP on one port).
# `mcp` transitively brings starlette, pydantic, httpx, anyio.
# `uvicorn` replaces Flask's Werkzeug dev server as the process runner.
# `asgiref` bridges Flask (WSGI) into the ASGI app tree.
# `mcp` is pinned below 2.0: the 2.0 SDK renamed `FastMCP` -> `MCPServer` and
# moved it from `mcp.server.fastmcp` to `mcp.server.mcpserver`, which breaks
# `mcp_tools.py` and the StreamableHTTPASGIApp wiring in praxis_local_api.py.
mcp>=1.3.0,<2.0
uvicorn>=0.30.0
websockets>=13.0
asgiref>=3.8.0

# Windows pseudo-terminal (ConPTY) support for the Claude Code interactive
# terminal (ChatPtySession). Only needed on Windows — noop on Unix/macOS.
pywinpty>=2.0.0; sys_platform == "win32"

# EXTERNAL RUNTIME DEPENDENCY (not pip-installable): the Praxis Local API wraps
# the `claude` CLI (Claude Code), which the user installs and authenticates
# out-of-band, e.g.:
#     npm install -g @anthropic-ai/claude-code   # install
#     claude login                               # one-time OAuth
# The Praxis Local API only discovers `claude` on PATH (or via CHAT_CLAUDE_BIN);
# it never installs or manages it. If absent, /health reports
# claude_available: false rather than failing.
